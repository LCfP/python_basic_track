% Activate the following line by filling in the right side. If for example the name of the root file is Main.tex, write
% "...root = Main.tex" if the chapter file is in the same directory, and "...root = ../Main.tex" if the chapter is in a subdirectory.
 
%!TEX root =  ../../python_basic_track.tex

\chapter{Lists \& Dictionaries}

	\section{List}
	% you have already seen a string, that is actually just a list of characters, so some of the same ideas apply, lets see:
	
		\subsection{Slicing}
		
		\subsection{Searching (and other useful functions)}
		
		
		
	\section{For}\index{for}
	
		\subsection[For vs Foreach]{(Experienced Programmers) For vs Foreach}\index{foreach|see {for}}
		
	\section{Dictionaries}
	
		% mention iteritems()?
	
	\section{Apply these concepts}
	
		\subsection{A day at the supermarket}