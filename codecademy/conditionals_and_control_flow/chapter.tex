% Activate the following line by filling in the right side. If for example the name of the root file is Main.tex, write
% "...root = Main.tex" if the chapter file is in the same directory, and "...root = ../Main.tex" if the chapter is in a subdirectory.
 
%!TEX root =  ../../python_basic_track.tex

\chapter{Conditionals and Control Flow}

	\section{Comparators}
	
	%Emphasis = != == ;)
	
	\section{To be and/or not to be}
	
	%A more readable truth table might not be a bad idea...
	
		\subsection{Lazy evaluation}
		
		%when evaluating everything in your mind, when can you stop 'early' i.e. if there is an or and we can quickly evaluate one side to true we are done, similarly if we can quickly evaluate one side of and to False the and will be False
	
	\section{If, elif, else}
		% note, there is no switch! writing an if elif elif elif elif else is the way to do it in python...
		
	\section{Apply these concepts}
	
		\section{Pyglatin}
		
		%Fine exercise in my opinion